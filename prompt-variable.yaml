- name: Variable value from prompt
  hosts: localhost
  vars_prompt:
  - name:  username
    prompt: Enter your name
    private: false
  - name: password
    prompt: Enter your password    
    private: true
  tasks:
    -name: printing info
    debug:
      msg: I am my name is {{username}} and my {{password}}