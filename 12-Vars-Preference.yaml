- name: Checking Vars Preferences
  hosts: localhost
  vars:
    Name: Ramesh
    Wishes: Morning from Play Level
  #vars_files:
  #-  vars.yaml    
  vars_prompt:
  - name: Wishes
    prompt: Enter wishes from prompt level
    private: false
  tasks:
    - name: checking Vars preferences
      #vars:
      #  Wishes: Morning from task level
      debug:
        msg: "Hello Ramesh, Good {{Wishes}}"     

#Arguments Level
#Task level
